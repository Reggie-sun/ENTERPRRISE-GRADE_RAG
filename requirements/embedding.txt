# 本地 embedding 服务依赖。
# Local embedding service dependencies.
# 请根据 CUDA 运行时单独安装 torch，例如：
# Install torch separately to match your CUDA runtime, for example:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124

fastapi==0.116.1  # FastAPI 框架 / FastAPI framework
uvicorn[standard]==0.35.0  # ASGI 服务器 / ASGI server
orjson  # 快速 JSON 库 / Fast JSON library
pydantic==2.11.7  # 数据验证库 / Data validation library
python-dotenv==1.1.1  # 环境变量加载 / Environment variable loader
FlagEmbedding==1.3.5  # BGE embedding 模型库 / BGE embedding model library
